#Enable or disable log notifications from Death Detangler.
#When enabled, the mod will log information about detected revival clones and compatibility fixes.
enableLogNotifications = true
#Automatically run cleanup when the server starts.
#This will scan and remove orphan clones on server startup.
autoRunOnStart = true
#If true, periodic cleanup will automatically remove clones.
#If false, periodic cleanup will only scan and log.
autoRemove = true
#Enable verbose logging for cleanup operations.
#When enabled, detailed information about each cleanup operation will be logged.
verboseLogging = false
#Interval in ticks between periodic cleanup runs.
#Default: 0 (disabled). Cleanup runs on server start, player death/clone, and logout only.
#Range: > 0
cleanIntervalTicks = 0

